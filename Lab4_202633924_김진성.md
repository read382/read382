# Lab 4

# 1. Linux

Linux는 1991년 **Linus Torvalds**가 처음 공개한 오픈소스 Unix 계열 운영체제 및 커널이다.

대표적인 Linux 배포판에는 다음과 같은 것들이 있다.

* Debian
* Fedora
* Ubuntu

Linux는 서버에서 많이 사용되며, 웹 서버에서도 널리 사용된다. 또한 임베디드 시스템과 모바일 시스템에서도 사용되며, 대표적으로 **Android**가 있다.

Linux는 오픈소스 소프트웨어 개발에서 가장 많이 사용되는 플랫폼 중 하나이며, 보안성과 안정성이 높은 운영체제로 알려져 있다.

Linux는 주로 **CLI(Command Line Interface)** 를 통해 사용할 수 있지만, 대부분의 배포판에서는 **GUI(Graphical User Interface)** 도 지원한다.

---

# 2. Kernel and Shell

## Kernel

**Kernel**은 운영체제의 핵심 부분으로, 하드웨어 자원을 제어하고 하드웨어와 통신하는 역할을 한다.

간단하게 말하면 사용자가 직접 하드웨어를 제어하는 것이 아니라 **Kernel이 운영체제와 하드웨어 사이에서 연결해 준다.**

## Shell

**Shell**은 사용자가 Kernel과 소통할 수 있도록 해주는 인터페이스이다.

사용자는 Shell을 통해 명령어를 입력하고 프로그램을 실행할 수 있다.

대표적인 Shell에는 다음과 같은 것들이 있다.

* `bash`
* `zsh`

즉, 기본적인 관계를 정리하면 다음과 같다.

```text
User
  ↓
Shell
  ↓
Kernel
  ↓
Hardware
```

---

# 3. CLI vs GUI

## CLI (Command Line Interface)

CLI는 명령어를 직접 입력해서 컴퓨터를 사용하는 방식이다.

* 명령어를 기억해야 한다.
* 주로 키보드를 사용한다.
* 비교적 빠르게 작업할 수 있다.
* Script를 이용하여 작업을 자동화할 수 있다.
* 입력한 명령어를 기록하고 다시 사용할 수 있다.
* 개발자에게 기본적인 작업 환경으로 많이 사용된다.

## GUI (Graphical User Interface)

GUI는 그래픽을 이용하여 컴퓨터를 사용하는 방식이다.

* 사용하기 쉽고 직관적이다.
* 주로 마우스를 사용하고 일부 키보드 단축키를 사용한다.
* CLI에 비해 상대적으로 느릴 수 있다.
* 반복적인 작업을 직접 수행해야 하는 경우가 있다.
* 일반적인 일상 사용자에게 적합하다.

### CLI와 GUI 비교

| CLI               | GUI          |
| ----------------- | ------------ |
| 명령어를 기억해야 함       | 사용하기 쉽고 직관적  |
| 주로 키보드 사용         | 주로 마우스 사용    |
| 비교적 빠름            | 상대적으로 느림     |
| Script를 통한 자동화 가능 | 반복 작업을 직접 수행 |
| 개발자에게 기본적인 환경     | 일반 사용자에게 적합  |

---

# 4. Run Shell Terminal

Shell을 사용하려면 터미널을 실행해야 한다.

### Linux / macOS

앱에서 **Terminal**을 검색하여 실행한다.

### Windows

**Git Bash**를 설치하여 사용할 수 있다.

---

# 5. Basic Shell Commands

## `pwd`

현재 작업 중인 디렉터리의 경로를 보여주는 명령어이다.

```bash
pwd
```
---

## `cd`

디렉터리를 변경할 때 사용하는 명령어이다.

```bash
cd [directoryname]
```

주요 경로 표현:

* `.` : 현재 디렉터리
* `..` : 상위 디렉터리
* `~` : 현재 사용자의 홈 디렉터리
* `/` : 루트 디렉터리
* `/[directory name]` : 절대 경로
* `./[directory name]` : 현재 디렉터리를 기준으로 하는 상대 경로
* `../[directory name]` : 상위 디렉터리를 기준으로 하는 상대 경로

---

## `ls`

현재 디렉터리의 파일과 디렉터리를 보여주는 명령어이다.

```bash
ls
```

---

# 6. More on `ls`

`ls`는 디렉터리의 내용을 확인할 때 자주 사용하는 명령어이다.

| Command           | Result                           |
| ----------------- | -------------------------------- |
| `ls`              | 현재 작업 디렉터리의 파일을 보여준다.            |
| `ls /bin`         | `/bin` 디렉터리의 파일을 보여준다.           |
| `ls -l`           | 현재 디렉터리의 파일을 긴 형식으로 보여준다.        |
| `ls -l /etc /bin` | `/etc`와 `/bin`의 파일을 긴 형식으로 보여준다. |
| `ls -la ..`       | 상위 디렉터리의 모든 파일을 긴 형식으로 보여준다.     |

## Long Format

`ls -l`을 사용하면 다음과 같은 정보를 확인할 수 있다.

* File Permissions : 파일 권한
* Owner : 소유자
* Group : 그룹
* Size : 파일 크기
* Modification Time : 수정 시간
* File Name : 파일 이름

---

# 7. Useful Tips

## Autocompletion

명령어나 파일 이름을 입력할 때 `Tab` 키를 누르면 자동 완성을 사용할 수 있다.

## Previous Commands

키보드의 `↑` 방향키를 누르면 이전에 입력했던 명령어를 다시 불러올 수 있다.

## `clear`

터미널 화면에 표시된 내용을 지우는 명령어이다.

```bash
clear
```

---

# 8. File and Directory Manipulation

파일이나 디렉터리를 복사, 이동, 삭제하는 명령어이다.

> **Warning:** 이 명령어들은 파일을 삭제하거나 덮어쓸 수 있으므로 중요한 파일은 미리 백업해야 한다.

---

## `cp`

`cp`는 파일과 디렉터리를 복사할 때 사용하는 명령어이다.

| Command             | Result                                                       |
| ------------------- | ------------------------------------------------------------ |
| `cp file1 file2`    | `file1`의 내용을 `file2`로 복사한다. `file2`가 없으면 새로 생성하고, 존재하면 덮어쓴다. |
| `cp -i file1 file2` | `file2`가 존재하는 경우 덮어쓰기 전에 사용자에게 확인한다.                         |
| `cp file1 dir1`     | `file1`을 `dir1` 디렉터리 안으로 복사한다.                               |
| `cp -r dir1 dir2`   | `dir1` 디렉터리와 그 안의 내용을 `dir2`로 복사한다.                          |

---

## `mv`

`mv`는 파일이나 디렉터리를 이동하거나 이름을 변경할 때 사용하는 명령어이다.

| Command               | Result                                                              |
| --------------------- | ------------------------------------------------------------------- |
| `mv file1 file2`      | `file2`가 없으면 `file1`의 이름을 `file2`로 변경한다. `file2`가 존재하면 덮어쓴다.        |
| `mv -i file1 file2`   | `file2`가 존재하는 경우 덮어쓰기 전에 사용자에게 확인한다.                                |
| `mv file1 file2 dir1` | `file1`과 `file2`를 `dir1` 디렉터리로 이동한다.                                |
| `mv dir1 dir2`        | `dir2`가 없으면 `dir1`의 이름을 `dir2`로 변경하고, 존재하면 `dir1`을 `dir2` 안으로 이동한다. |

---

## `rm`

`rm`은 파일이나 디렉터리를 삭제할 때 사용하는 명령어이다.

| Command             | Result                                  |
| ------------------- | --------------------------------------- |
| `rm file1 file2`    | `file1`과 `file2`를 삭제한다.                 |
| `rm -i file1 file2` | 각 파일을 삭제하기 전에 사용자에게 확인한다.               |
| `rm -r dir1 dir2`   | `dir1`과 `dir2` 디렉터리 및 그 안의 모든 내용을 삭제한다. |

> **주의:** `rm`으로 삭제한 파일은 쉽게 복구할 수 없다. 특히 와일드카드와 함께 사용할 때 주의해야 한다.

---

## `mkdir`

새로운 디렉터리를 만드는 명령어이다.

```bash
mkdir directoryname
```

---

# 9. Wildcards

와일드카드(wildcard)를 사용하면 여러 파일을 한 번에 지정할 수 있다.

| Pattern   | Meaning                     |
| --------- | --------------------------- |
| `*`       | 모든 파일 이름                    |
| `g*`      | `g`로 시작하는 모든 파일             |
| `b*.txt`  | `b`로 시작하고 `.txt`로 끝나는 파일    |
| `Data???` | `Data` 뒤에 정확히 3개의 문자가 있는 파일 |

---

# 10. Caution with `rm`

`rm`은 특히 주의해서 사용해야 한다.

Linux에서는 `rm`으로 삭제한 파일을 일반적인 방법으로 되돌리는 명령어가 없다. 특히 `*` 같은 와일드카드와 같이 사용하면 의도하지 않은 파일까지 삭제할 수 있다.

따라서 삭제하기 전에 먼저 `ls`로 어떤 파일이 선택되는지 확인하는 것이 좋다.

---

# 11. Help Commands

## `help`

Shell에서 사용할 수 있는 명령어의 도움말을 확인할 때 사용한다.

```bash
help cd
```

이렇게 사용하면 `cd` 명령어의 사용 방법과 옵션 등을 확인할 수 있다.

## `man`

manual의 약자로 특정 명령어의 자세한 사용법과 설명을 확인할 때 사용한다.

```bash
man ls
```

위 명령어를 입력하면 `ls` 명령어에 대한 매뉴얼을 확인할 수 있다.

---

# 12. Exiting Terminal

## `exit`

터미널을 종료할 때 사용한다.

```bash
exit
```
